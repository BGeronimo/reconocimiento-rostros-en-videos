<UserControl x:Class="ReconocimientoFacial.Views.EnrollmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReconocimientoFacial.Views"
             xmlns:viewmodels="clr-namespace:ReconocimientoFacial.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
             
    <UserControl.DataContext>
        <viewmodels:EnrollmentViewModel />
    </UserControl.DataContext>

    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <!-- Izquierda: Feed de CÃ¡mara y miniaturas -->
        <Grid Grid.Column="0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <!-- Video background effect -->
            <Border Grid.Row="0" Margin="0,0,0,15" Background="#0C1322" CornerRadius="12" BorderBrush="#1C2E4A" BorderThickness="1">
                <Grid>
                    <!-- Feed real -->
                    <Border CornerRadius="12" ClipToBounds="True">
                        <Image Source="{Binding CameraFrame}" Stretch="Uniform" />
                    </Border>
                    <Border CornerRadius="12" Background="#E50C1322" Panel.ZIndex="10">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSelectionMode}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="SELECCIONE LA FOTO DE PERFIL" Foreground="#00F2FF" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                            <ListBox ItemsSource="{Binding CapturedFaces}" SelectedItem="{Binding SelectedFace}" Background="Transparent" BorderThickness="0" HorizontalAlignment="Center" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal" MaxWidth="350"/>
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="100" Height="100" Margin="5" CornerRadius="8" ClipToBounds="True" BorderBrush="#1C2E4A" BorderThickness="2">
                                            <Image Source="{Binding}" Stretch="UniformToFill" />
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="Margin" Value="0"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Opacity" Value="1"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="False">
                                                <Setter Property="Opacity" Value="0.4"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                            </ListBox>
                            <Button Content="CONFIRMAR SELECCIÓN" Command="{Binding FinalizeRegistrationCommand}" Margin="0,30,0,0" Height="45" Width="220" Background="#00F2FF" Foreground="#0C1322" FontWeight="Bold" FontSize="14" BorderThickness="0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </Border>
                    <!-- Overlay Decorativo (Corners) -->
                    <Path Stroke="#00F2FF" StrokeThickness="1" 
                          Data="M 20,40 L 20,20 L 40,20 M 20,260 L 20,280 L 40,280 M 520,20 L 540,20 L 540,40 M 540,260 L 540,280 L 520,280" 
                          HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Stretch="Fill" Margin="30"/>
                    <TextBlock Text="CALIBRATING OPTICS" Foreground="#00F2FF" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,40" FontSize="10" />
                    <TextBlock Text="{Binding CaptureProgress}" Foreground="#CE9EFF" FontSize="120" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.8"/>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#CF9FFF" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,10" FontSize="14" />
                </Grid>
            </Border>

            <!-- Bottom thumbnails -->
            <Border Grid.Row="1" Background="#0C1322" CornerRadius="12" BorderBrush="#1C2E4A" BorderThickness="1" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    
                    <ItemsControl ItemsSource="{Binding CapturedFaces}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="45" Height="45" Background="#142135" CornerRadius="8" BorderThickness="2" BorderBrush="#CF9FFF" Margin="0,0,10,0" ClipToBounds="True">
                                    <Image Source="{Binding}" Stretch="UniformToFill" />
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    
                    <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="CAPTURED" Foreground="#555" FontSize="10" HorizontalAlignment="Right"/>
                        <TextBlock Foreground="#00F2FF" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right">
                            <Run Text="0"/>
                            <Run Text="{Binding CaptureProgress}"/>
                            <Run Text=" / 09" Foreground="#555" />
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Derecha: Controles de Formulario -->
        <StackPanel Grid.Column="1" Margin="10,10,0,10">
            <Border Background="#0C1322" CornerRadius="12" Padding="20" BorderBrush="#1C2E4A" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="User Registration" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0,0,0,5"/>
                    <TextBlock Text="Map biometric hash to employee profile" Foreground="#7A8DAB" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,30"/>
                    
                    <TextBlock Text="EMPLOYEE NAME" Foreground="#555" FontSize="10" Margin="0,0,0,10" />
                    <!-- Mock TextBox styling with Border -->
                    <Border BorderBrush="#1C2E4A" BorderThickness="0,0,0,1" Margin="0,0,0,25">
                        <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" Foreground="White" BorderThickness="0" Height="30" FontSize="14"/>
                    </Border>

                    <TextBlock Text="CORPORATE ID" Foreground="#555" FontSize="10" Margin="0,0,0,10" />
                    <!-- Mock TextBox styling with Border -->
                    <Border BorderBrush="#1C2E4A" BorderThickness="0,0,0,1" Margin="0,0,0,25">
                        <TextBox Text="{Binding EmployeeCode, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" Foreground="White" BorderThickness="0" Height="30" FontSize="14"/>
                    </Border>

                    <Grid Margin="0,20,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button Content="CLEAR" Grid.Column="0" Margin="0,0,10,0" Height="40" Background="Transparent" Foreground="#555" BorderBrush="#1C2E4A" BorderThickness="1" />
                        <Button Content="CONFIRM PROFILE" Command="{Binding StartCaptureCommand}" Grid.Column="1" Height="40" Background="#E2B1FF" Foreground="#32064C" FontWeight="Bold" BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Background="#0C1322" CornerRadius="12" Padding="15" Margin="0,20,0,0" BorderBrush="#1C2E4A" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="â“˜" Foreground="#00F2FF" FontSize="18" VerticalAlignment="Top" Margin="0,0,10,0"/>
                    <StackPanel>
                        <TextBlock Text="Auto-Calibration Enabled" Foreground="White" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,5"/>
                        <TextBlock Text="System will automatically adjust focal depth for optimal biometric point cloud generation during the countdown." Foreground="#7A8DAB" FontSize="10" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>

