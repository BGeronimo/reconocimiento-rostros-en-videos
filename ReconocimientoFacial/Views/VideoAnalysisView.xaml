<UserControl x:Class="ReconocimientoFacial.Views.VideoAnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReconocimientoFacial.Views"
             xmlns:vm="clr-namespace:ReconocimientoFacial.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="850" Background="Transparent"
             AllowDrop="True" Drop="UserControl_Drop">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <!-- Custom ScrollBar and Styles could be added here -->
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <!-- Center Area -->
            <ColumnDefinition Width="280"/>
            <!-- Right Sidebar Area -->
        </Grid.ColumnDefinitions>

        <!-- Center Area -->
        <Grid Grid.Column="0" Margin="20,0,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <!-- Player -->
                <RowDefinition Height="Auto"/>
                <!-- Tracklist / Config -->
            </Grid.RowDefinitions>

            <!-- Optional Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="VIDEO ANALYSIS" Foreground="#7A8DAB" FontWeight="SemiBold" FontSize="18"/>
                        <TextBlock Text="Arrástre y suelte archivos de video .mp4 aquí o use el explorador..." Foreground="#555" FontSize="12"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="Explorar Archivos" Background="#2A2F45" Foreground="White" Padding="15,5" Command="{Binding OpenFilesCommand}" BorderThickness="0" Margin="0,0,0,0" VerticalAlignment="Center"/>
                </Grid>
            </StackPanel>

            <!-- Video Player Display -->
            <Border Grid.Row="1" Background="#0A111E" CornerRadius="12" BorderBrush="#112A4F" BorderThickness="1" ClipToBounds="True">
                <Grid>
                    <!-- Visual feed -->
                    <Image Source="{Binding CurrentFrame}" Stretch="Uniform" />

                    <!-- Overlays: ENGINE OPTIMIZATION mock -->
                    <Border VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="20" Background="#1A0D26" BorderBrush="#2A2F45" BorderThickness="1" CornerRadius="12" Padding="15,10" Opacity="0.9">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="⯐" Foreground="#CF9FFF" FontSize="14" Margin="0,0,5,0"/>
                                <TextBlock Text="ENGINE OPTIMIZATION" Foreground="#CF9FFF" FontSize="10" FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="GPU Acceleration" Foreground="White" FontSize="12" Width="100"/>
                                <ToggleButton IsChecked="True" BorderThickness="0" Background="#00F2FF" Width="30" Height="15" Margin="20,0,0,0">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="7.5"/>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="FRAME SKIP" Foreground="#7A8DAB" FontSize="10" Width="100"/>
                                <TextBlock Text="0.04S" Foreground="#00F2FF" FontSize="10" FontWeight="Bold" Margin="20,0,0,0"/>
                            </StackPanel>
                            <ProgressBar Value="30" Height="4" Margin="0,5,0,0" Foreground="#00F2FF" Background="#2A2F45" BorderThickness="0"/>
                        </StackPanel>
                    </Border>
                    
                    <TextBlock Text="ESPERANDO VIDEO..." Foreground="#555" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsVideoLoaded, Converter={StaticResource BoolToVis}, FallbackValue=Visible}" x:Name="WaitingText"/>
                </Grid>
            </Border>

            <!-- Video File List and Controls at bottom -->
            <Grid Grid.Row="2" Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <!-- File list -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Play button -->
                </Grid.ColumnDefinitions>

                <ListBox Background="Transparent" BorderThickness="0" ItemsSource="{Binding VideoList}" SelectedItem="{Binding SelectedVideo}" MinHeight="60" MaxHeight="120" AlternationCount="2">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#111824" Margin="0,0,0,5" CornerRadius="5" Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding FileName}" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                        <TextBlock Text="{Binding InfoString}" Foreground="#7A8DAB" FontSize="10"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding FileSize}" VerticalAlignment="Center" Foreground="#CF9FFF" FontSize="11" Margin="10,0,10,0"/>

                                    <Button Grid.Column="2" Content="✕" Foreground="#FF4C4C" Background="Transparent" BorderThickness="0"
                                            VerticalAlignment="Center" FontSize="14" Cursor="Hand" Margin="5,0,0,0"
                                            Command="{Binding DataContext.RemoveVideoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" ToolTip="Remover video"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Bd" Background="Transparent" SnapsToDevicePixels="true">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#2A2F45"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#1A0D26"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <!-- Play/Stop Controls -->
                <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                    <Button Content="▶ ANALIZAR" Background="#CF9FFF" Foreground="#32064C" FontWeight="Bold" Padding="15,10" Command="{Binding StartAnalysisCommand}" BorderThickness="0" Margin="0,0,0,10"/>
                    <Button Content="⏹ DETENER" Background="#2A2F45" Foreground="White" Padding="15,10" Command="{Binding StopAnalysisCommand}" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Right Sidebar Area (Data Stream) -->
        <Border Grid.Column="1" Background="#050B14" BorderBrush="#112A4F" BorderThickness="1,0,0,0" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Status Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="DATA STREAM" Foreground="White" FontSize="16" FontWeight="SemiBold" />
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <Ellipse Width="6" Height="6" Fill="#00F2FF" Margin="0,0,5,0"/>
                        <TextBlock Text="ANALYSIS ACTIVE" Foreground="#00F2FF" FontSize="10"/>
                    </StackPanel>
                </StackPanel>

                <!-- Counters -->
                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Identified -->
                    <Border Grid.Column="0" Background="#1A1C29" CornerRadius="8" Padding="15" Margin="0,0,5,0">
                        <StackPanel>
                            <TextBlock Text="IDENTIFIED" Foreground="#7A8DAB" FontSize="10"/>
                            <TextBlock Text="{Binding IdentifiedCount}" Foreground="#CF9FFF" FontSize="26" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>

                    <!-- Unknown -->
                    <Border Grid.Column="1" Background="#1A1C29" CornerRadius="8" Padding="15" Margin="5,0,0,0">
                        <StackPanel>
                            <TextBlock Text="UNKNOWN" Foreground="#7A8DAB" FontSize="10"/>
                            <TextBlock Text="{Binding UnknownCount}" Foreground="#00F2FF" FontSize="26" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Recent Hits Title -->
                <TextBlock Grid.Row="2" Text="RECENT HITS" Foreground="#7A8DAB" FontSize="10" FontWeight="Bold" Margin="0,0,0,10"/>

                <!-- Scrollable Hits List -->
                <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding RecentHits}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#111824" CornerRadius="8" Margin="0,0,0,8" Padding="10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="50"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Photo -->
                                        <Border CornerRadius="25" Width="40" Height="40" VerticalAlignment="Center">
                                            <Border.Background>
                                                <ImageBrush ImageSource="{Binding FaceSnapshot}" Stretch="UniformToFill"/>
                                            </Border.Background>
                                        </Border>

                                        <!-- Info -->
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                                            <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="SemiBold" FontSize="13"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                <TextBlock Text="{Binding ClearanceLevel}" Foreground="{Binding ClearanceColor}" FontSize="10"/>
                                                <TextBlock Text=" • " Foreground="#555" FontSize="10"/>
                                                <TextBlock Text="{Binding Timestamp}" Foreground="#7A8DAB" FontSize="10"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Match % -->
                                        <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                            <TextBlock Text="{Binding CertaintyText}" Foreground="{Binding ClearanceColor}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Right"/>
                                            <TextBlock Text="☑" Foreground="{Binding ClearanceColor}" FontSize="12" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Processing Stream Bar at Bottom -->
                <StackPanel Grid.Row="4" Margin="0,20,0,0">
                    <TextBlock Text="PROCESSING STREAM..." Foreground="#555" FontSize="10" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                    <ProgressBar Value="{Binding CurrentProgress}" Minimum="0" Maximum="100" Height="2" Background="#112A4F" Foreground="#00F2FF" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
